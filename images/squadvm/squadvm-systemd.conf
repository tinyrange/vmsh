[Manager]
# Keep systemd's boot state visible on the VM serial console until the native
# frontend presents the first complete desktop frame.
ShowStatus=yes
StatusUnitFormat=combined
LogTarget=console
LogLevel=info
LogColor=yes
