name: Publish Neurodesktop image

on:
  workflow_dispatch:
    inputs:
      neurocontainers_ref:
        description: Neurocontainers branch, tag, or commit to build
        required: true
        default: main
        type: string
      tag:
        description: GHCR image tag
        required: true
        default: edge
        type: string
      publish_latest:
        description: Also update the latest tag
        required: true
        default: false
        type: boolean

permissions:
  contents: read
  packages: write

concurrency:
  group: neurodesktop-image-${{ inputs.tag }}
  cancel-in-progress: false

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/neurodesktop-glass

jobs:
  build:
    name: Build ${{ matrix.platform }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 360
    strategy:
      fail-fast: false
      matrix:
        include:
          - architecture: x86_64
            platform: linux/amd64
            suffix: amd64
            runner: ubuntu-24.04
          - architecture: aarch64
            platform: linux/arm64
            suffix: arm64
            runner: ubuntu-24.04-arm

    steps:
      - name: Validate tag
        env:
          IMAGE_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          if [[ ! "$IMAGE_TAG" =~ ^[A-Za-z0-9_][A-Za-z0-9_.-]{0,113}$ ]]; then
            echo "::error::Invalid container tag: $IMAGE_TAG"
            exit 1
          fi

      - name: Reclaim runner disk space
        run: |
          set -euo pipefail
          for path in \
            /usr/local/lib/android \
            /usr/local/.ghcup \
            /usr/share/dotnet \
            /opt/ghc \
            /opt/hostedtoolcache/CodeQL
          do
            if [[ -d "$path" ]]; then
              sudo find "$path" -mindepth 1 -depth -delete
            fi
          done
          docker system prune --all --force
          df -h /

      - name: Checkout Neurocontainers
        uses: actions/checkout@v6
        with:
          repository: neurodesk/neurocontainers
          ref: ${{ inputs.neurocontainers_ref }}
          path: neurocontainers
          persist-credentials: false

      - name: Checkout vmsh image tools
        uses: actions/checkout@v6
        with:
          path: vmsh
          persist-credentials: false
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: neurocontainers/pyproject.toml

      - name: Install Neurocontainers builder
        run: python -m pip install ./neurocontainers

      - name: Restore download cache
        uses: actions/cache@v5
        with:
          path: neurocontainers/httpcache
          key: neurodesktop-http-${{ matrix.architecture }}-${{ hashFiles('neurocontainers/recipes/neurodesktop-lite/build.yaml') }}
          restore-keys: |
            neurodesktop-http-${{ matrix.architecture }}-

      - name: Generate architecture-specific build context
        id: context
        working-directory: neurocontainers
        env:
          ARCHITECTURE: ${{ matrix.architecture }}
        run: |
          set -euo pipefail
          python -m builder stage neurodesktop-lite \
            --architecture "$ARCHITECTURE" \
            --recreate \
            --download
          dockerfile="$(find build -mindepth 2 -maxdepth 2 -type f -path '*/neurodesktop-lite*/*' -iname '*.dockerfile' -print -quit)"
          if [[ -z "$dockerfile" ]]; then
            echo "::error::Neurocontainers did not generate a Dockerfile"
            exit 1
          fi
          context="$(dirname "$dockerfile")"
          echo "dockerfile=neurocontainers/$dockerfile" >> "$GITHUB_OUTPUT"
          echo "context=neurocontainers/$context" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push backwards-compatible architecture image
        uses: docker/build-push-action@v6
        with:
          context: ${{ steps.context.outputs.context }}
          file: ${{ steps.context.outputs.dockerfile }}
          build-contexts: |
            neurocontainer-cache=${{ steps.context.outputs.context }}/cache
          platforms: ${{ matrix.platform }}
          push: true
          tags: ${{ env.IMAGE }}:${{ inputs.tag }}-${{ matrix.suffix }}
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.title=Neurodesktop Glass
          cache-from: type=registry,ref=${{ env.IMAGE }}:buildcache-${{ matrix.suffix }}
          cache-to: type=registry,ref=${{ env.IMAGE }}:buildcache-${{ matrix.suffix }},mode=max,ignore-error=true

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: vmsh/cc/go.mod
          cache-dependency-path: vmsh/cc/go.sum

      - name: Install OCI publishing tool
        working-directory: vmsh/cc
        run: |
          set -euo pipefail
          go install github.com/google/go-containerregistry/cmd/crane@v0.21.8
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: Repack and push enhanced eStargz architecture image
        working-directory: vmsh/cc
        env:
          IMAGE_TAG: ${{ inputs.tag }}
          ARCH_SUFFIX: ${{ matrix.suffix }}
          PLATFORM: ${{ matrix.platform }}
        run: |
          set -euo pipefail
          work_dir="$(mktemp -d "$RUNNER_TEMP/estargz.XXXXXX")"
          crane pull \
            --platform "$PLATFORM" \
            --format=oci \
            "$IMAGE:${IMAGE_TAG}-${ARCH_SUFFIX}" \
            "$work_dir/source"
          go run ./cmd/estargz-repack-image \
            "$work_dir/source" \
            "$work_dir/enhanced"
          crane push \
            "$work_dir/enhanced" \
            "$IMAGE:${IMAGE_TAG}-estargz-${ARCH_SUFFIX}"
          crane validate --remote "$IMAGE:${IMAGE_TAG}-estargz-${ARCH_SUFFIX}"
          df -h /

  manifest:
    name: Publish multi-architecture manifest
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Publish manifest
        env:
          IMAGE_TAG: ${{ inputs.tag }}
          PUBLISH_LATEST: ${{ inputs.publish_latest }}
        run: |
          set -euo pipefail
          sources=(
            "$IMAGE:${IMAGE_TAG}-amd64"
            "$IMAGE:${IMAGE_TAG}-arm64"
          )
          enhanced_sources=(
            "$IMAGE:${IMAGE_TAG}-estargz-amd64"
            "$IMAGE:${IMAGE_TAG}-estargz-arm64"
          )
          docker buildx imagetools create \
            --tag "$IMAGE:$IMAGE_TAG" \
            "${sources[@]}"
          docker buildx imagetools create \
            --tag "$IMAGE:${IMAGE_TAG}-estargz" \
            "${enhanced_sources[@]}"
          if [[ "$PUBLISH_LATEST" == "true" ]]; then
            docker buildx imagetools create \
              --tag "$IMAGE:latest" \
              "${sources[@]}"
            docker buildx imagetools create \
              --tag "$IMAGE:latest-estargz" \
              "${enhanced_sources[@]}"
          fi
          docker buildx imagetools inspect "$IMAGE:$IMAGE_TAG"
          docker buildx imagetools inspect "$IMAGE:${IMAGE_TAG}-estargz"
